<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:color="@color/secondary_text" android:state_focused="true"/>
    <!-- Default/unfocused state (white) -->
    <item android:color="@color/secondary_text"/>
</selector>
